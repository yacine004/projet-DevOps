apiVersion: v1
kind: Service
metadata:
  name: brasilburger-service
  namespace: production
spec:
  selector:
    app: brasilburger
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
